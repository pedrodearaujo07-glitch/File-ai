name: voice_file_ai
description: IA que move e edita arquivos no celular por comando de voz.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  speech_to_text: ^7.0.0
  permission_handler: ^11.3.1
  shared_preferences: ^2.2.3
  cupertino_icons: ^1.0.6

flutter:
  uses-material-design: true
  
